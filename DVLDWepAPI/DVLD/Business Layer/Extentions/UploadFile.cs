using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Hosting;
using System;
using System.IO;
using System.Threading.Tasks;

namespace Business_Layer.Extentions
{
    public class FileUploadHelper
    {/*
        private readonly IWebHostEnvironment _webHostEnvironment;

        public FileUploadHelper(IWebHostEnvironment webHostEnvironment)
        {
            _webHostEnvironment = webHostEnvironment;
        }

      

        public void DeleteImage(string fileName, string subFolder = "people")
        {
            if (string.IsNullOrEmpty(fileName)) return;

            var filePath = Path.Combine(_webHostEnvironment.WebRootPath, "uploads", subFolder, fileName);

            if (File.Exists(filePath))
            {
                try { File.Delete(filePath); } catch { *//* سجل الخطأ هنا *//* }
            }
        }*/
    }
}
